import type { Contribution } from "@/lib/types";
export function ContributionPreview({ did, fingerprint, url, description, announcement, saved }: { did: string; fingerprint: string; url: string; description: string; announcement: string; saved?: Contribution }) { return <section className="rounded-xl border border-zinc-800 bg-zinc-900/30 p-5"><p className="text-xs uppercase tracking-wider text-zinc-500">{saved ? "Saved proof" : "Live proof preview"}</p><p className="mt-4 break-all font-mono text-xs text-zinc-400">DID: {did}</p><p className="mt-2 font-mono text-xs text-zinc-500">Fingerprint: {fingerprint}</p><p className="mt-4 text-xs text-zinc-500">Room: technocore</p><p className="mt-4 text-xs text-zinc-500">Announcement:</p><p className="mt-2 text-sm leading-6 text-zinc-300">{announcement || "Your signed announcement will appear here."}</p><p className="mt-5 text-xs text-zinc-500">Status: <span className={saved ? "text-emerald-300" : "text-zinc-400"}>{saved ? `Posted, sequence ${saved.sequence}` : "Not posted yet"}</span></p>{url && description && <p className="mt-4 text-xs text-zinc-600">{url} · {description}</p>}</section>; }
