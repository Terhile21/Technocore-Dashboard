import type { Contribution } from "@/lib/types";
import { ProofCard } from "@/components/contributions/ProofCard";
export function ContributionList({ items, onCopy, onCopySequence, onPrimary, onArchive, onDelete, onRecordAnother }: { items: Contribution[]; onCopy: (item: Contribution) => void; onCopySequence: (item: Contribution) => void; onPrimary: (id: string) => void; onArchive: (id: string) => void; onDelete: (id: string) => void; onRecordAnother: () => void }) { const visible = items.filter((item) => !item.isArchived); return <div className="space-y-3">{visible.length === 0 ? <div className="rounded-xl border border-dashed border-zinc-800 p-10 text-center text-sm text-zinc-600">No contributions recorded yet.</div> : visible.map((item) => <ProofCard key={item.id} item={item} onCopy={() => onCopy(item)} onCopySequence={() => onCopySequence(item)} onPrimary={() => onPrimary(item.id)} onArchive={() => onArchive(item.id)} onDelete={() => onDelete(item.id)} onRecordAnother={onRecordAnother} />)}</div>; }
