import { Copy, ExternalLink, Star } from "lucide-react";
import type { Contribution } from "@/lib/types";
export function proofLine(item: Contribution): string { return `Contribution: ${item.publicUrl}\nAgent DID: ${item.did}\nSigned Technocore record: room ${item.room}, sequence ${item.sequence ?? "pending"}`; }
export function proofDetails(item: Contribution): string { return `${proofLine(item)}\nNonce: ${item.nonce}`; }
export function ProofCard({ item, onCopy, onCopySequence, onPrimary, onArchive, onDelete, onRecordAnother }: { item: Contribution; onCopy: () => void; onCopySequence: () => void; onPrimary: () => void; onArchive: () => void; onDelete: () => void; onRecordAnother: () => void }) { const hasSequence = item.sequence != null; return <article className="rounded-xl border border-emerald-400/25 bg-emerald-400/5 p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-semibold text-emerald-300">Contribution recorded</p><p className="mt-2 text-xs text-zinc-400">{item.source ?? "other"} · Room: {item.room} · Sequence: {item.sequence ?? "pending"} · Nonce: <span className="font-mono">{item.nonce}</span></p>{!hasSequence && <p className="mt-1 text-xs text-amber-300">Sequence not yet confirmed — copy is disabled until it is.</p>}</div>{item.isPrimary && <Star size={17} className="fill-emerald-400 text-emerald-400" />}</div><a href={item.publicUrl} target="_blank" rel="noreferrer" className="mt-4 flex items-center gap-2 break-all text-sm text-zinc-200 hover:text-emerald-300">{item.publicUrl}<ExternalLink size={14} className="shrink-0" /></a><p className="mt-2 text-sm leading-6 text-zinc-400">{item.description}</p><div className="mt-5 flex flex-wrap gap-2"><button onClick={onCopy} disabled={!hasSequence} title={!hasSequence ? "Sequence not confirmed yet" : undefined} className="inline-flex items-center gap-2 rounded-lg border border-zinc-700 px-3 py-2 text-xs text-zinc-300 disabled:cursor-not-allowed disabled:opacity-40"><Copy size={14} />Copy proof</button><button onClick={onCopySequence} disabled={!hasSequence} title={!hasSequence ? "Sequence not confirmed yet" : undefined} className="inline-flex items-center gap-2 rounded-lg border border-zinc-700 px-3 py-2 text-xs text-zinc-300 disabled:cursor-not-allowed disabled:opacity-40"><Copy size={14} />Copy sequence</button><button onClick={onPrimary} className="inline-flex items-center gap-2 rounded-lg border border-zinc-700 px-3 py-2 text-xs text-zinc-300"><Star size={14} />{item.isPrimary ? "Primary contribution" : "Mark as primary"}</button><button onClick={onArchive} className="rounded-lg border border-zinc-700 px-3 py-2 text-xs text-zinc-300">Archive</button><button onClick={onDelete} className="rounded-lg border border-red-400/20 px-3 py-2 text-xs text-red-300">Delete local</button><button onClick={onRecordAnother} className="rounded-lg border border-zinc-700 px-3 py-2 text-xs text-zinc-300">Record another</button></div></article>; }
