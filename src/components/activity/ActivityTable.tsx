import { Copy, ExternalLink } from "lucide-react";
import type { SignedActivity } from "@/lib/types";
import { ActivityDetails } from "@/components/activity/ActivityDetails";
export function ActivityTable({ items, onCopy, onIntro }: { items: SignedActivity[]; onCopy: (item: SignedActivity) => void; onIntro: (item: SignedActivity) => void }) { return <div className="divide-y divide-zinc-800">{items.map((item) => <article key={item.id} className="py-4 first:pt-0"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-start"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><span className="rounded bg-zinc-800 px-2 py-1 text-[10px] uppercase tracking-wider text-emerald-300">{item.type}</span><span className="text-xs text-zinc-500">{item.room} · #{item.sequence}</span><span className="text-xs text-zinc-600">{new Date(item.timestamp).toLocaleString()}</span></div><p className="mt-2 break-words text-sm text-zinc-300">{item.text}</p><ActivityDetails item={item} /></div><div className="flex shrink-0 gap-2"><button onClick={() => onCopy(item)} title="Copy proof line" aria-label="Copy proof line" className="rounded-md border border-zinc-700 p-2 text-zinc-400 hover:text-zinc-200"><Copy size={14} /></button>{item.room === "lobby" && item.type !== "intro" && <button onClick={() => onIntro(item)} className="rounded-md border border-zinc-700 px-2 py-1.5 text-xs text-zinc-400 hover:text-zinc-200">Mark intro</button>}<a href={`https://technocore.chat/r/${item.room}`} target="_blank" rel="noreferrer" title="Open room" aria-label="Open room" className="rounded-md border border-zinc-700 p-2 text-zinc-400 hover:text-zinc-200"><ExternalLink size={14} /></a></div></div></article>)}</div>; }
