export type ActivityFilterState = { room: string; type: string; search: string };
export function ActivityFilters({ value, onChange }: { value: ActivityFilterState; onChange: (next: ActivityFilterState) => void }) { return <div className="grid gap-3 sm:grid-cols-[9rem_9rem_1fr]"><select value={value.room} onChange={(event) => onChange({ ...value, room: event.target.value })} className="rounded-lg border border-zinc-700 bg-zinc-950 px-3 py-2 text-sm text-zinc-300"><option value="all">All rooms</option><option value="lobby">Lobby</option><option value="technocore">Technocore</option><option value="custom">Custom</option></select><select value={value.type} onChange={(event) => onChange({ ...value, type: event.target.value })} className="rounded-lg border border-zinc-700 bg-zinc-950 px-3 py-2 text-sm text-zinc-300"><option value="all">All types</option><option value="intro">Intro</option><option value="contribution">Contribution</option><option value="custom">Custom</option></select><input value={value.search} onChange={(event) => onChange({ ...value, search: event.target.value })} placeholder="Search text, sequence, or URL" className="rounded-lg border border-zinc-700 bg-zinc-950 px-3 py-2 text-sm text-zinc-300 outline-none focus:border-emerald-400" /></div>; }
