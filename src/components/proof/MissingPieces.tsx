import Link from "next/link";
export function MissingPieces({ hasIntro, hasContribution, hasSequence }: { hasIntro: boolean; hasContribution: boolean; hasSequence: boolean }) { const gaps = []; if (!hasIntro) gaps.push(["No lobby introduction found. Post one from the dashboard.", "/"]); if (!hasContribution) gaps.push(["No contribution recorded yet.", "/contributions"]); if (!hasSequence) gaps.push(["This contribution has no sequence. Re-record it.", "/contributions"]); if (!gaps.length) return null; return <section className="rounded-xl border border-amber-400/20 bg-amber-400/5 p-5"><h2 className="font-semibold text-amber-200">What is missing</h2><div className="mt-3 space-y-3">{gaps.map(([text, href]) => <div key={text} className="flex flex-col justify-between gap-2 text-sm text-amber-100/80 sm:flex-row sm:items-center"><span>{text}</span><Link href={href} className="shrink-0 text-xs font-semibold text-amber-300 hover:text-amber-200">Go there</Link></div>)}</div></section>; }
