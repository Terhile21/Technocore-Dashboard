import { CheckCircle2, Circle } from "lucide-react";
export function ProofStatus({ checks }: { checks: { label: string; complete: boolean }[] }) { const ready = checks[0]?.complete && checks[1]?.complete && checks[2]?.complete; const basic = checks[0]?.complete && checks.some((item) => item.complete); return <section className="rounded-xl border border-zinc-800 bg-zinc-900/30 p-5"><div className="flex items-center justify-between"><h2 className="font-semibold text-zinc-100">Completeness</h2><span className={ready ? "text-emerald-300" : basic ? "text-amber-300" : "text-zinc-500"}>{ready ? "Ready to share" : basic ? "Basic proof" : "Incomplete"}</span></div><div className="mt-4 grid gap-3 sm:grid-cols-2">{checks.map((item) => <div key={item.label} className="flex items-center gap-2 text-sm text-zinc-400">{item.complete ? <CheckCircle2 className="text-emerald-400" size={16} /> : <Circle className="text-zinc-700" size={16} />}{item.label}</div>)}</div></section>; }
