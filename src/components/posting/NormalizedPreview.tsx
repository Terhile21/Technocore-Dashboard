import { buildSigningPayload, normalizeText } from "@/lib/technocore";
export function NormalizedPreview({ room, text, nonce = "<generated nonce>" }: { room: string; text: string; nonce?: string }) { return <div className="rounded-lg border border-zinc-800 bg-zinc-950/70 p-3"><p className="text-xs uppercase tracking-wider text-zinc-600">Normalized payload preview</p><p className="mt-2 break-all font-mono text-xs leading-5 text-zinc-400">{buildSigningPayload(room, nonce, text)}</p><p className="mt-2 text-xs text-zinc-600">Final text: {normalizeText(text).length}/4096 characters</p></div>; }
