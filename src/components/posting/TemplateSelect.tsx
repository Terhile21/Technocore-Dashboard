import { TEMPLATES, type TemplateId } from "@/lib/technocore/templates";
export function TemplateSelect({ value, onChange }: { value: TemplateId | "custom"; onChange: (value: TemplateId | "custom") => void }) { return <label className="text-sm text-zinc-300">Template<select value={value} onChange={(event) => onChange(event.target.value as TemplateId | "custom")} className="mt-2 w-full rounded-lg border border-zinc-700 bg-zinc-950 px-3 py-2.5 text-sm outline-none focus:border-emerald-400"><option value="custom">Custom message</option>{Object.entries(TEMPLATES).map(([id, template]) => <option key={id} value={id}>{template.label}</option>)}</select></label>; }
