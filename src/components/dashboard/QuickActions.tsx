import Link from "next/link";
import { Clipboard, FilePlus2, MessageSquare, RefreshCw } from "lucide-react";

export function QuickActions({ locked, hasActivity, onIntro, onRefresh, onCopy }: { locked: boolean; hasActivity: boolean; onIntro: () => void; onRefresh: () => void; onCopy: () => void }) {
  return <section><h2 className="text-sm font-semibold text-zinc-200">Quick actions</h2><div className="mt-3 flex flex-wrap gap-2"><button disabled={locked} onClick={onIntro} title={locked ? "Unlock DID first" : "Post an introduction"} className="inline-flex items-center gap-2 rounded-lg bg-emerald-400 px-3 py-2.5 text-sm font-semibold text-zinc-950 disabled:cursor-not-allowed disabled:opacity-40"><MessageSquare size={16} />Post Introduction</button><Link href="/contributions" className="inline-flex items-center gap-2 rounded-lg border border-zinc-700 px-3 py-2.5 text-sm text-zinc-300 hover:border-zinc-500"><FilePlus2 size={16} />Record Contribution</Link><button disabled={locked} onClick={onRefresh} title={locked ? "Unlock DID first" : "Refresh activity"} className="inline-flex items-center gap-2 rounded-lg border border-zinc-700 px-3 py-2.5 text-sm text-zinc-300 hover:border-zinc-500 disabled:cursor-not-allowed disabled:opacity-40"><RefreshCw size={16} />Refresh Activity</button><button disabled={!hasActivity || locked} onClick={onCopy} title={!hasActivity ? "No activity yet" : locked ? "Unlock DID first" : "Copy proof summary"} className="inline-flex items-center gap-2 rounded-lg border border-zinc-700 px-3 py-2.5 text-sm text-zinc-300 hover:border-zinc-500 disabled:cursor-not-allowed disabled:opacity-40"><Clipboard size={16} />Copy Proof Summary</button></div></section>;
}
