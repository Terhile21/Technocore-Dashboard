import Link from "next/link";
import { LockKeyhole } from "lucide-react";
export function UnlockCard({ onUnlock }: { onUnlock: () => void }) { return <section className="flex flex-col items-center rounded-2xl border border-zinc-800 bg-zinc-900/30 px-6 py-12 text-center"><LockKeyhole className="text-amber-300" size={28} /><h2 className="mt-4 text-lg font-semibold text-zinc-100">Unlock your DID to continue</h2><p className="mt-2 max-w-md text-sm leading-6 text-zinc-500">Your encrypted identity is here, but private signing keys remain locked after refresh.</p><div className="mt-5 flex gap-2"><button onClick={onUnlock} className="rounded-lg bg-emerald-400 px-4 py-2.5 text-sm font-semibold text-zinc-950">Unlock DID</button><Link href="/dids" className="rounded-lg border border-zinc-700 px-4 py-2.5 text-sm text-zinc-300">Manage DIDs</Link></div></section>; }
