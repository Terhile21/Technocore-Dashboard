import { CheckCircle2, Circle } from "lucide-react";
export function Checklist({ items }: { items: { label: string; complete: boolean }[] }) { return <section className="rounded-xl border border-zinc-800 bg-zinc-900/30 p-5"><h2 className="text-sm font-semibold text-zinc-200">Getting started</h2><div className="mt-4 space-y-3">{items.map((item) => <div key={item.label} className="flex items-center gap-3 text-sm"><span className={item.complete ? "text-emerald-400" : "text-zinc-700"}>{item.complete ? <CheckCircle2 size={17} /> : <Circle size={17} />}</span><span className={item.complete ? "text-zinc-300" : "text-zinc-500"}>{item.label}</span></div>)}</div></section>; }
