import { Check, Copy, LockKeyhole } from "lucide-react";

export function shortenDid(did: string): string { return did.length > 28 ? `${did.slice(0, 18)}...${did.slice(-8)}` : did; }
export function StatusStrip({ did, fingerprint, unlocked, onCopy, onUnlock }: { did: string; fingerprint: string; unlocked: boolean; onCopy: () => void; onUnlock: () => void }) {
  return <section className="flex flex-col gap-4 rounded-xl border border-zinc-800 bg-zinc-900/50 p-4 sm:flex-row sm:items-center sm:justify-between"><div className="min-w-0"><div className="flex items-center gap-2"><span className="text-xs uppercase tracking-[0.18em] text-zinc-500">Acting as</span><button onClick={onCopy} title="Copy DID" aria-label="Copy DID" className="text-zinc-500 hover:text-emerald-300"><Copy size={14} /></button></div><p className="mt-1 truncate font-mono text-sm text-zinc-200">{shortenDid(did)}</p><p className="mt-1 font-mono text-xs text-zinc-600">Fingerprint {fingerprint}</p></div><div className="flex items-center gap-3"><span className={unlocked ? "flex items-center gap-2 text-xs text-emerald-400" : "flex items-center gap-2 text-xs text-amber-300"}><LockKeyhole size={14} />{unlocked ? "Unlocked" : "Locked"}</span>{!unlocked && <button onClick={onUnlock} className="rounded-lg bg-emerald-400 px-3 py-2 text-xs font-semibold text-zinc-950 hover:bg-emerald-300">Unlock DID</button>}</div></section>;
}
export function CopyToast({ visible }: { visible: boolean }) { return visible ? <span className="inline-flex items-center gap-1 text-xs text-emerald-400"><Check size={13} />Copied</span> : null; }
