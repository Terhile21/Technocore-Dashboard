import Link from "next/link";
import type { SignedActivity } from "@/lib/types";
function relativeTime(value: string): string { const seconds = Math.max(1, Math.round((Date.now() - new Date(value).getTime()) / 1000)); if (seconds < 60) return `${seconds}s ago`; if (seconds < 3600) return `${Math.round(seconds / 60)}m ago`; if (seconds < 86400) return `${Math.round(seconds / 3600)}h ago`; return `${Math.round(seconds / 86400)}d ago`; }
export function RecentActivity({ activities, error, onRetry }: { activities: SignedActivity[]; error?: string; onRetry: () => void }) { return <section className="rounded-xl border border-zinc-800 bg-zinc-900/30 p-5"><div className="flex items-center justify-between"><div><h2 className="text-sm font-semibold text-zinc-200">Recent activity</h2><p className="mt-1 text-xs text-zinc-600">Your latest signed records</p></div><Link href="/activity" className="text-xs text-emerald-400 hover:text-emerald-300">View all</Link></div>{error ? <div className="py-8 text-center"><p className="text-sm text-red-300">{error}</p><button onClick={onRetry} className="mt-3 text-xs text-emerald-400">Retry</button></div> : activities.length === 0 ? <div className="py-8 text-center"><p className="text-sm text-zinc-300">No signed activity yet</p><p className="mt-2 text-xs text-zinc-600">Post an introduction in the lobby to create your first verifiable record.</p></div> : <div className="mt-4 divide-y divide-zinc-800">{activities.slice(0, 8).map((item) => <div key={item.id} className="flex items-start justify-between gap-4 py-3 first:pt-0"><div className="min-w-0"><div className="flex items-center gap-2"><span className="rounded bg-zinc-800 px-2 py-1 text-[10px] uppercase tracking-wider text-emerald-300">{item.type}</span><span className="text-xs text-zinc-600">{item.room} · #{item.sequence}</span></div><p className="mt-2 truncate text-sm text-zinc-300">{item.text}</p></div><span className="shrink-0 text-xs text-zinc-600">{relativeTime(item.timestamp)}</span></div>)}</div>}</section>; }
