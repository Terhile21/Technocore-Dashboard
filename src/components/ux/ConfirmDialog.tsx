"use client";
import { AlertTriangle, X } from "lucide-react";
export function ConfirmDialog({ title, message, confirmLabel = "Confirm", onConfirm, onClose }: { title: string; message: string; confirmLabel?: string; onConfirm: () => void; onClose: () => void }) { return <div className="fixed inset-0 z-40 flex items-center justify-center bg-black/70 p-4" role="dialog" aria-modal="true" aria-labelledby="confirm-title"><div className="w-full max-w-md rounded-xl border border-zinc-700 bg-zinc-900 p-6"><div className="flex items-start justify-between"><div className="flex gap-3"><AlertTriangle className="mt-0.5 text-amber-300" size={19} /><div><h2 id="confirm-title" className="font-semibold text-zinc-100">{title}</h2><p className="mt-2 text-sm leading-6 text-zinc-400">{message}</p></div></div><button onClick={onClose} aria-label="Close" title="Close" className="text-zinc-500 hover:text-zinc-200"><X size={17} /></button></div><div className="mt-6 flex justify-end gap-2"><button onClick={onClose} className="min-h-11 rounded-lg border border-zinc-700 px-3 text-sm text-zinc-300">Cancel</button><button onClick={onConfirm} className="min-h-11 rounded-lg bg-red-400 px-3 text-sm font-semibold text-zinc-950">{confirmLabel}</button></div></div></div>; }
