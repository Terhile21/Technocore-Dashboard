import Link from "next/link";
import { LockKeyhole } from "lucide-react";
export function LockedState({ onUnlock }: { onUnlock: () => void }) { return <section className="rounded-2xl border border-zinc-800 bg-zinc-900/30 p-10 text-center"><LockKeyhole className="mx-auto text-amber-300" size={26} /><h2 className="mt-4 font-semibold text-zinc-100">Unlock this DID to continue</h2><p className="mt-2 text-sm text-zinc-500">Your actions are disabled until the local signing session is unlocked.</p><div className="mt-5 flex justify-center gap-2"><button onClick={onUnlock} className="min-h-11 rounded-lg bg-emerald-400 px-4 text-sm font-semibold text-zinc-950">Unlock DID</button><Link href="/dids" className="inline-flex min-h-11 items-center rounded-lg border border-zinc-700 px-4 text-sm text-zinc-300">Manage DIDs</Link></div></section>; }
